// Caminho: C:\Users\MarcoErapha\backend\src\domain\repositories\UserRepository.js

// Exporta a classe UserRepository como uma exportação padrão (default)
export default class UserRepository {
  // Método abstrato para encontrar todos os usuários
  findAll() {
    throw new Error("Método não implementado.");
  }
  // Método abstrato para encontrar um usuário por ID
  findById(id) {
    throw new Error("Método não implementado.");
  }
  // Método abstrato para encontrar um usuário por email
  findByEmail(email) {
    throw new Error("Método não implementado.");
  }
  // Método abstrato para criar um novo usuário
  create(user) {
    throw new Error("Método não implementado.");
  }
  // Método abstrato para atualizar um usuário
  update(id, user) {
    throw new Error("Método não implementado.");
  }
  // Método abstrato para excluir um usuário por ID
  delete(id) {
    throw new Error("Método não implementado.");
  }
}
